## Build

```bash
git clone https://github.com/geoje/ClaudeCodeUsage.git
cd ClaudeCodeUsage
xcodebuild -project ClaudeCodeUsage.xcodeproj -scheme ClaudeCodeUsage build
```

Requires Xcode (with command line tools installed).
